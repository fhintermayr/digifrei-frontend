import { TrainerCreationDto } from './trainer-creation-dto';

describe('TrainerCreationDto', () => {
  it('should create an instance', () => {
    expect(new TrainerCreationDto()).toBeTruthy();
  });
});
