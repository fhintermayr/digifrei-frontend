import { ApprenticeCreationDto } from './apprentice-creation-dto';

describe('ApprenticeCreationDto', () => {
  it('should create an instance', () => {
    expect(new ApprenticeCreationDto()).toBeTruthy();
  });
});
