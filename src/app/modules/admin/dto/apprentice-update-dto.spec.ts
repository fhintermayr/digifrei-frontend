import { ApprenticeUpdateDto } from './apprentice-update-dto';

describe('ApprenticeUpdateDto', () => {
  it('should create an instance', () => {
    expect(new ApprenticeUpdateDto()).toBeTruthy();
  });
});
