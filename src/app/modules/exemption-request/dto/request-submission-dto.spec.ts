import { RequestSubmissionDto } from './request-submission-dto';

describe('RequestSubmissionDto', () => {
  it('should create an instance', () => {
    expect(new RequestSubmissionDto()).toBeTruthy();
  });
});
