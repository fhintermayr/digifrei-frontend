import { RequestProcessingUpdateDto } from './request-processing-update-dto';

describe('RequestProcessingUpdateDto', () => {
  it('should create an instance', () => {
    expect(new RequestProcessingUpdateDto()).toBeTruthy();
  });
});
