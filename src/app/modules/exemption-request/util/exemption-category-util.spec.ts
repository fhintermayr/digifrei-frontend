import { ExemptionCategoryUtil } from './exemption-category-util';

describe('ExemptionCategoryUtil', () => {
  it('should create an instance', () => {
    expect(new ExemptionCategoryUtil()).toBeTruthy();
  });
});
