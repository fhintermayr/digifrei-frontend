import { ProcessingStatusUtil } from './processing-status-util';

describe('ProcessingStatusUtil', () => {
  it('should create an instance', () => {
    expect(new ProcessingStatusUtil()).toBeTruthy();
  });
});
