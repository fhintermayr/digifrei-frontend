import { FormatProcessingStatusPipe } from './format-processing-status.pipe';

describe('FormatProcessingStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatProcessingStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
