import { FormatExemptionCategoryPipe } from './format-exemption-category.pipe';

describe('FormatExemptionCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatExemptionCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
